name: Build EA7300v2

on: [workflow_dispatch]

permissions: write-all

env:
  BUILD_TARGET: 25.12.4
  GCCVER: 14.3.0
  LLVMBPFVER: 21.1.6

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@main
    
    - name: Initialize environment
      env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      run: |
        sudo apt update 
        sudo apt install build-essential clang flex bison g++ gawk gcc-multilib g++-multilib \
        gettext git libncurses5-dev libssl-dev python3-setuptools rsync unzip zlib1g-dev \
        file wget

    - name: Install toolchain
      run: |
        cd $GITHUB_WORKSPACE
        wget https://downloads.openwrt.org/releases/$BUILD_TARGET/targets/ramips/mt7621/openwrt-toolchain-${{ env.BUILD_TARGET }}-ramips-mt7621_gcc-${{ env.GCCVER }}_musl.Linux-x86_64.tar.zst
        tar --zstd -xf openwrt-toolchain-*.tar.zst
        rsync -a openwrt-toolchain-${{ env.BUILD_TARGET }}-ramips-mt7621_gcc-${{ env.GCCVER }}_musl.Linux-x86_64/* .
        ./scripts/ext-toolchain.sh --toolchain toolchain-mipsel_24kc_gcc-${{ env.GCCVER }}_musl --overwrite-config --config ramips/mt7621
        wget https://downloads.openwrt.org/releases/$BUILD_TARGET/targets/ramips/mt7621/llvm-bpf-${{ env.LLVMBPFVER }}.Linux-x86_64.tar.zst
        tar --zstd -xf llvm*.tar.zst
        ln -s llvm-bpf-${{ env.LLVMBPFVER }}.Linux-x86_64 llvm-bpf
        echo "CONFIG_BPF_TOOLCHAIN_PREBUILT=y" >> .config
        
    - name: Update feeds
      run: cd $GITHUB_WORKSPACE && ./scripts/feeds update -a
      
    - name: Install feeds
      run: cd $GITHUB_WORKSPACE && ./scripts/feeds install -a

    - name: Make defconfig
      run: |
        cd $GITHUB_WORKSPACE
        echo "CONFIG_TARGET_ramips_mt7621_DEVICE_linksys_ea7300-v2=y" >> .config
        echo "CONFIG_PACKAGE_luci=y" >> .config
        echo "CONFIG_PACKAGE_luci-ssl=y" >> .config
        echo "CONFIG_PACKAGE_luci-app-advanced-reboot=y" >> .config
        echo "CONFIG_PACKAGE_uboot-envtools=y" >> .config
        make defconfig
        
    - name: Build Image
      run: |
        cd $GITHUB_WORKSPACE
        make -j$(nproc) V=s

    - name: Upload directories
      uses: actions/upload-artifact@main
      with:
        name: build_upload
        path: |
          bin

    - name: Upload files
      uses: maggie44/actions/packages/automatic-releases@built-packages
      with:
        repo_token: "${{ secrets.GITHUB_TOKEN }}"
        automatic_release_tag: EA7300v2-${{ env.BUILD_TARGET }}
        prerelease: false
        title: EA7300v2 ${{ env.BUILD_TARGET }}
        files: |
          bin/targets/ramips/mt7621/*.bin
